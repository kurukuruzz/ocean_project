import styled from "styled-components";

export default function Navigation() {
  return (
    <Container>
      네비게이션바
    </Container>
  );
}

const Container = styled.div`
  display: flex;
  flex-direction: row;
`